<section id="content-deatails" class="row clearfix">
  <article id="details-atticle" class="col-sm-8">
    <h1 id="article-title">Look at our animals</h1>
  </article>
</section>

</details>


<div class="row">
    <% @products.each do |product| %>
    <div class="col-sm-6 col-md-4 col-md-offset-1">
      <div class="thumbnail thumb-box">
        <img src="<%= asset_path product.image_name %>" alt="..." height="400x">
        <div class="caption">
          <h3><%= product.title %></h3>
          <p style="text-align:center;"><%= product.info %></p>
          <a href="<%= product_path(product) %>"><button class="btn btn-buy" style="text-align:center;">Buy Now!</button></a>
        </div>
      </div>
    </div>
  <% end  %>
</div>
